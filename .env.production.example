# ============================================================
# 프로덕션(CloudType) 환경변수 예시
# 실제 값은 CloudType 프로젝트의 "환경변수"에 입력하세요. 이 파일은 커밋용 예시입니다.
# ============================================================

# [필수] PostgreSQL 연결 문자열
#   CloudType Postgres 템플릿 사용 시 내부 호스트명으로 연결 (예: postgresql://user:pass@postgres:5432/mijin?schema=public)
#   외부 매니지드 DB(Neon/Supabase 등)면 sslmode=require 를 붙이세요.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/mijin?schema=public"

# [필수] JWT 세션 서명 키 — 32자 이상. 생성: openssl rand -hex 32
AUTH_SECRET="change-me-with-openssl-rand-hex-32"


# [선택] 외부 cron 백업이 /api/internal/auto-assign 을 호출할 때 사용하는 시크릿
#   설정하지 않아도 인프로세스 워커(30초 주기)가 자동배정을 수행합니다.
CRON_SECRET=""

# [선택] 카카오 로컬 API 키 (주소↔좌표 지오코딩). 없으면 OSM Nominatim 무키 폴백 → 그래도 실패 시 수동 입력.
KAKAO_REST_API_KEY=""

# [선택] SMS 발송 방식: console(로그만) | solapi(실발송). 미설정 시 발송 생략.
SMS_PROVIDER="console"

# [선택] SMS_PROVIDER=solapi 일 때만 필요
SOLAPI_API_KEY=""
SOLAPI_API_SECRET=""
SOLAPI_SENDER=""

# [권장] 확인요망 알림을 받을 관리자 휴대폰 번호. 쉼표로 여러 개.
#   자동배정이 처리하지 못한 접수(후보 없음·지역 판별 불가·무응답 회수·거절)를 문자로 알립니다.
#   비워 두면 관리자 계정에 등록된 전화번호로 보내는데, 시드로 만든 관리자 계정의 번호는
#   01000000000 이라 아무에게도 닿지 않습니다. 운영 개시 전에 실제 담당자 번호를 넣으세요.
ADMIN_ALERT_PHONES=""

# [중요] 개인기술자 가입 휴대폰 본인인증 — NHN KCP 본인확인(V2) 직접 연동. 실서비스는 반드시 kcp.
#   mock 은 입력값을 그대로 통과시키므로 프로덕션에서 쓰면 안 됩니다.
#   아래 값은 모두 서버가 런타임에 읽습니다 — NEXT_PUBLIC_* 로 넣지 마세요.
#   NEXT_PUBLIC_ 값은 `next build` 시점에 번들로 인라인되는데 CloudType 은 환경변수를 컨테이너
#   런타임에만 주입해서, 빌드 산출물에 빈 값이 박혀 "본인인증 설정이 없습니다"로 죽습니다.
#   브라우저는 /api/identity/config 로 provider 이름만 받고, 거래등록은 서버(/api/identity/kcp/start)가 합니다.
IDENTITY_PROVIDER="kcp"
# KCP 상점관리자(partner.kcp.co.kr) > 기술관리센터 > 인증센터 > 가맹점 인증키관리
#   사이트코드(예: PO046)와 가맹점 인증키(ENC_KEY). 둘 다 필수(하나라도 비면 인증 시작 시 502).
#   ⚠️ 인증키를 재발급하면 이 값도 같이 바꿔야 한다(옛 키로는 거래등록·복호화가 전부 실패).
KCP_SITE_CD=""
KCP_ENC_KEY=""
# 부가서비스 > 휴대폰본인확인 > 웹사이트코드 설정 (12자). DI(중복가입확인정보) 생성 기준 — 바꾸면 기존 DI 대조가 깨진다.
KCP_WEB_SITEID=""
# 운영 서버(기본 https://cert.kcp.co.kr). 비워 두면 운영. 테스트는 https://testcert.kcp.co.kr + 사이트코드 AO7F3.
# KCP_API_BASE=""
# 앱의 공개 원점(실도메인). ① KCP 가 인증 결과를 보낼 Ret_URL 의 원점 — 상점관리자 「부가서비스 > 휴대폰본인확인 >
#   인증결과URL 설정」에 등록된 도메인이어야 하며(미등록은 KCP 가 거부) 프로덕션에서 비우면 인증 시작이 막힌다(fail-closed).
#   ② 만족도 조사 문자 링크의 원점(src/lib/survey.ts). CloudType 임시 도메인이 아니라 실도메인.
APP_BASE_URL="https://xn--ok0bp94bnc26kra.com"
# 상점관리자 「연동방식 설정」의 신규 연동방식(V2) 사용여부가 '사용'이어야 한다(미사용이면 CS24 오류).
#
# (참고) 포트원 경유(IDENTITY_PROVIDER=portone)는 실연동 채널에 포트원 결제카드 등록이 필수라 2026-09 부터 쓰지 않는다.
#   되살릴 때만 아래 셋을 채운다.
# PORTONE_STORE_ID=""
# PORTONE_CHANNEL_KEY=""
# PORTONE_API_SECRET=""
# [선택] CI/DI 저장용 해시 키. CI·DI 는 평문으로 보관하지 않고 이 키로 HMAC-SHA256 해시만
#   저장한다(src/lib/identity/hash.ts). 미설정 시 AUTH_SECRET 을 쓴다.
#   ⚠️ 한 번 정하면 바꾸지 말 것 — 키가 바뀌면 이전 해시와 대조가 끊겨 "같은 사람" 판정이 깨진다.
#   AUTH_SECRET 을 교체할 계획이 있다면 이 값을 별도로 고정해 두는 편이 안전하다.
IDENTITY_HASH_SECRET=""
# [탈출구] 프로덕션 빌드에서 IDENTITY_PROVIDER 가 kcp/portone 이 아니면 가입이 통째로 막힌다
#   (mock 은 입력값을 그대로 통과시켜 무인증 가입이 열리므로 fail-closed 로 둔다).
#   실서비스 배포본을 본인인증 없이 로컬 점검할 때만 1 로 켠다. 운영에는 절대 넣지 말 것.
# ALLOW_MOCK_IDENTITY="1"

# [선택] 서버 STT (음성 녹음 → 텍스트 자동 변환): gemini | openai.
#   미설정 시 변환만 생략되고 녹음 재생은 그대로 동작.
STT_PROVIDER="gemini"
# gemini 사용 시 (키 발급: https://aistudio.google.com/apikey)
GEMINI_API_KEY=""
# GEMINI_STT_MODEL="gemini-2.5-flash"           # 기본값
# openai 사용 시
# OPENAI_API_KEY=""
# OPENAI_BASE_URL="https://api.openai.com/v1"   # Groq 등 호환 서비스로 교체 가능
# OPENAI_STT_MODEL="whisper-1"

# [선택] 컨테이너 최초 기동 시 관리자/샘플 업체 계정 시드 (1이면 실행, upsert라 재실행 안전)
#   최초 배포 후에는 제거하거나 비워두세요.
RUN_DB_SEED="0"

# [권장] Cloudflare R2 — 현장 사진·음성 녹음·사업자등록증·전기공사업 등록증 저장소.
#   넷 중 하나라도 비면 DB(StoredFile)에 저장된다. 버킷은 별도로 생성해야 한다.
#   R2 사용 시 신규 음성/증빙의 DB에는 파일 ID·MIME·R2 키만 저장한다.
#   기존 DB/파일시스템 파일은 계속 조회 가능하며 자동 이동하지 않는다.
#   현장 사진은 R2 일시 장애 시 유실 방지를 위해 DB로 긴급 저장한다.
#   버킷은 **비공개**로 둘 것. 사진은 관리자/배정자, 음성·증빙은 관리자만 열람한다.
#   공개 URL이나 커스텀 도메인을 붙이지 않는다.
#   키 발급: Cloudflare 대시보드 → R2 → "API 토큰 관리" → 오브젝트 읽기·쓰기 권한
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET=""
# R2_ENDPOINT=""   # 기본값 https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com

# [선택] 업로드 저장 루트. 기본 /app/uploads. 스토리지를 다른 경로(예: /data)에
#   마운트했다면 그 경로로 지정. 기동 로그의 "업로드 디렉터리 쓰기 가능" 여부로 확인.
# UPLOADS_DIR="/app/uploads"
